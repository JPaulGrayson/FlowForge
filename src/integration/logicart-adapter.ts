export class LogicArtAdapter { constructor(private config: {serverUrl: string}) {} async createSession(workflow: any) { const res = await fetch(this.config.serverUrl + "/api/remote/session", { method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify({name: workflow.name}) }); return res.json(); } } export function createLogicArtAdapter(config: any) { return new LogicArtAdapter(config); } export function workflowToLogicArt(w: any) { return { nodes: w.nodes, edges: w.edges }; }
