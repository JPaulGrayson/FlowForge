export class ParameterResolver { async resolve(param: any, ctx: any) { if (!param?.type) return param; if (param.type === "static") return param.value; if (param.type === "reference") return ctx[param.source?.source]?.[param.source?.inputName || param.source?.nodeId]; if (param.type === "template") return param.template; return param; } } export function createParameterResolver() { return new ParameterResolver(); }
